# Render Blueprint for PostgreSQL Database Only
# Deploy database first, then services one by one

databases:
  - name: scholara-postgres
    plan: starter
    ipAllowList: []
    databaseName: scholara_db
    user: scholara_user
    postgresMajorVersion: 15